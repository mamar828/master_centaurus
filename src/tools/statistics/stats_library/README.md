To run this C++ library, you must first build it as python-importable executable by running the build.sh script:
- cd into stats_library
- run ./build.sh

This will create a build/stats_library.so file that can be imported in python scripts.